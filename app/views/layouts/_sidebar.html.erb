
  <div class="ml-4" id="sidebarList">

    <div id="ml-0"> <%= t('Pl') %> </div>

    <%current_user.playlists.each do |pl| %>

      <div id="litem" class="row mt-1 border-bottom">
        <%= render partial: "layouts/playlistListItem", locals: { playlist: pl} %>
      </div>
    <% end %>


</div>
<div class="mt-2 ml-3 mr-2">
  <%= render 'layouts/playlistModal' %>
</div>
