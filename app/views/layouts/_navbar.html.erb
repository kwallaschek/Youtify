

<nav class="navbar navbar-expand-lg navbar-light bg-light">

  <a class="navbar-brand" href="#">Youtify</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to t('home'), root_path, class: "nav-link" %>
      </li>
    </ul>
    <% if !logged_in? %>
      <ul class="navbar-nav ml-0">
        <li class="nav-item margin-left=0">
          <%= link_to t('login'), login_path, class: "nav-link" %>
        </li>
        <li class="nav-item margin-left=0">
          <%= link_to t('sign up'), signup_path, class: "nav-link" %>
        </li>
      </ul>
    <% else %>
      <form class="form-inline my-2 my-lg-0" ml-3>
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit"><%= t('search')%></button>
      </form>
      <ul class="navbar-nav ml-0">
        <li class="nav-item margin-left=0">
          <%= link_to t('profile'), user_path(session[:user_id]), class: "nav-link"%>
        </li>
        <li class="nav-item margin-left=0">
          <%= link_to t('logout'), logout_path, class: "nav-link" , method: :delete %>
        </li>
      </ul>
    <% end %>
    <%= link_to '', url_for(locale: :de), class: "flag flag-de p-1"%>
    <%= link_to '', url_for(locale: :jp), class: "flag flag-jp p-1"%>
    <%= link_to '', url_for(locale: :en), class: "flag flag-us p-1"%>
  </div>
</nav>

